<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/products">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page">View Food item</li>
    </ol>
</nav>

<div class="view-user p-5">
    {{#each rows}}
    <div class="row mb-5">
        <div class="col text-center">
            <h3>{{this.food_name}}</h3>
        </div>
    </div>

    <div class="row">
        <div class="col">
            <table class="table table-bordered table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Type</th>
                        <th scope="col">Name</th>
                        <th scope="col">Storage conditions °C</th>
                        <th scope="col">Manifacturer</th>
                        <th scope="col">Price ($)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{this.food_type}}</td>
                        <td>{{this.food_name}}</td>
                        <td>{{this.food_storage_conditions}}</td>
                        <td>{{this.food_manufacturer}}</td>
                        <td>{{this.food_price}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="row">
            <div class="col">
                <h2 class="p-1">Compounds</h2>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="p-1">{{this.food_comment}}</div>
            </div>
        </div>
    </div>
    {{/each}}
</div>